# Server
PORT=3001
NODE_ENV=development

# Frontend origin allowed by CORS (comma separated). Use * to allow all.
CORS_ORIGIN=*

# MySQL connection
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Ssnigdho48.
DB_NAME=spymebaby

# JWT
JWT_SECRET=change_this_to_a_long_random_secret22228374
JWT_EXPIRES_IN=7d

# Default seeded admin (matches the credentials currently used by the frontend)
SEED_USERNAME=snigdho
SEED_PASSWORD=azsx1234
SEED_NAME=Snigdho
